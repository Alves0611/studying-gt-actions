name: Workflow com Variáveis

on:
  workflow_dispatch:

# 🌐 Variável de ambiente no nível do WORKFLOW
env:
  WORKFLOW_VAR: "Valor do Workflow"

jobs:
  build:
    runs-on: ubuntu-latest

    # 🧱 Variável de ambiente no nível do JOB
    env:
      JOB_VAR: "Valor do Job"

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Mostrar variáveis do Workflow e Job
        run: |
          echo "WORKFLOW_VAR = ${{ env.WORKFLOW_VAR }}"
          echo "JOB_VAR = ${{ env.JOB_VAR }}"

      - name: Variável no nível do Step (Action)
        env:
          STEP_VAR: "Valor do Step"
        run: |
          echo "WORKFLOW_VAR = ${{ env.WORKFLOW_VAR }}"
          echo "JOB_VAR = ${{ env.JOB_VAR }}"
          echo "STEP_VAR = $STEP_VAR"
