name: first-pipe

on:
  workflow_dispatch:

env:
  MY_ENV_VAR_WORKFLOW: Hello, world!

jobs:
  first-job:
    name: first-job
    runs-on: ubuntu-latest
    env:
      MY_ENV_VAR_JOB: test, package, release, and deploy your project.
    steps:
      - name: Run a one-line script
        run: echo "${{ env.MY_ENV_VAR_WORKFLOW }}"

      - name: Run a multi-line script
        run: |
          echo "Add other actions to build,"
          echo "${{ env.MY_ENV_VAR_JOB }}"

