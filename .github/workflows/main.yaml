name: estudando gerenciamento de ambientes
on:
  workflow_dispatch:
jobs:
  deploy-fake:
    runs-on: ubuntu-latest
    steps:
      - name: deploy em ambiente
        run: echo "Deploy no ambiente de ${{ vars.environment }}"
